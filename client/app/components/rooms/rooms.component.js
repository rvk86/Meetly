import template from './rooms.html';
import controller from './rooms.controller';
import './rooms.styl';

let roomsComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default roomsComponent;
